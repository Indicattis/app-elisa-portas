

### Plano: Remover seta (ChevronRight) do card

Remover o ícone de seta (`ChevronRight`) na linha 423 de `VendaPendentePedidoCard.tsx` e seu comentário.

**Arquivo**: `src/components/pedidos/VendaPendentePedidoCard.tsx`
- Deletar linhas 422-423 (`{/* Seta */}` e `<ChevronRight .../>`)
- Remover o import de `ChevronRight` se não for usado em outro lugar do arquivo

